using UnityEngine;

public class PlayerHealth : MonoBehaviour
{
    //[Header("Health Settings")]
    //public int hpleft = 4;

    //[Header("Body Parts")]
    //[SerializeField] private GameObject[] bodyParts = new GameObject[9];
    //[SerializeField] private GameObject[] bodyPartsDamaged = new GameObject[4];
    //public Material damagedMaterial;


    //// Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}

    //void partDamage(GameObject part)
    //{
    //    if (hpleft > 0)
    //    {
    //        foreach (GameObject bodyPart in bodyParts)
    //        {
    //            if (bodyPart.name == part.name)
    //            {
    //                part.GetComponent<Renderer>().material = damagedMaterial;
    //                bodyPartsDamaged.Add(bodyPart);
    //                bodyParts.Remove(bodyPart);
    //                hpleft--;
    //                break;
    //            }
    //        }
            
    //    }
    //}
}
